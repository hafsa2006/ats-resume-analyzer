# Production (Vercel / vite build) — Render backend URL, no trailing slash
VITE_API_URL=https://ats-resume-backend-k5ru.onrender.com

# Local development (optional — .env.development uses 127.0.0.1:5000)
# VITE_API_URL=http://127.0.0.1:5000
