# Production (Vercel) — Render backend origin only (no /api suffix)
VITE_API_URL=https://your-backend.onrender.com

# Local dev (optional .env.local overrides)
# Leave VITE_API_URL unset to use Vite proxy, or set directly:
# VITE_API_URL=http://127.0.0.1:5000
