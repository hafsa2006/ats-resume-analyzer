# Backend API origin (no trailing slash)

# Local development (copy to .env.development or .env):
VITE_API_URL=http://127.0.0.1:5000

# Production — set in Vercel project settings (do not commit real values):
# VITE_API_URL=https://your-backend.onrender.com
